# Quality Score Agent

Scores public photos for downstream grouping, hero selection, and narrative generation.

## API

- `GET /health`
- `POST /api/v1/quality-scores`

## Verification

```bash
PYTHON=/path/to/python scripts/verify.sh
```

